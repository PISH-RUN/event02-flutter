import 'package:flutter/material.dart';

class OurCard extends StatelessWidget {
  final Widget child;
  final EdgeInsets margin;
  final EdgeInsets padding;

  const OurCard({@required this.child, this.margin = const EdgeInsets.all(24), this.padding = const EdgeInsets.all(0), Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
